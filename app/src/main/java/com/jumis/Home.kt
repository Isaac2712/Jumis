package com.jumis

import android.content.Intent
import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle

class Home : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        //setContentView(R.layout.activity_home)


        /*
        --Paso 3
        val intent : Intent = intent
        val userName = intent.getStringExtra("Username")
        println(userName)

        --Printea lo que le manda un intent cuyo nombre es Username


        --Controlar permisos peligrosos (las diapositivas están obsoletas, el código no funciona)
        Código diapo. 70, comprueba si el permiso ha sido concedido y en el caso que no lo pide al usuario
        Código diapo. 71, escuchamos la respuesta del usuario


         */



    }
}